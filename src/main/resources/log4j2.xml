<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO" monitorInterval="30">

    <Properties>
        <Property name="App">doctors</Property>
        <Property name="localLogDir">logs</Property>
        <Property name="logDir">/var/logs</Property>
        <Property name="splitSize">30 MB</Property>
    </Properties>

    <Appenders>
        <RollingRandomAccessFile name="localLog" fileName="../${localLogDir}/${App}.log" immediateFlush="true"
                                 filePattern="${localLogDir}/$${date:yyyy-MM}/${App}-info-%d{yyyy-MM-dd}-%i.log.gz"
                                 append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd 'at' HH:mm:ss z} [%t] [${App}] %-5level %logger{36} %L %M - %msg%xEx%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="6" modulate="true"/>
                <SizeBasedTriggeringPolicy size="${splitSize}"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="NEUTRAL"/>
                <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
            <DefaultRolloverStrategy max="50"/>
        </RollingRandomAccessFile>

        <RollingRandomAccessFile name="infoLog" fileName="${logDir}/${App}.log" immediateFlush="true"
                                 filePattern="${logDir}/$${date:yyyy-MM}/${App}-%d{yyyy-MM-dd}-%i.log.gz"
                                 append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd 'at' HH:mm:ss z} [%t] [${App}] %-5level %logger{36} %L %M - %msg%xEx%n"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="6" modulate="true"/>
                <SizeBasedTriggeringPolicy size="${splitSize}"/>
            </Policies>
            <Filters>
                <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="NEUTRAL"/>
                <ThresholdFilter level="info" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
            <DefaultRolloverStrategy max="50"/>
        </RollingRandomAccessFile>
    </Appenders>

    <Loggers>
        <Root level="all" includeLocation="true" additivity="true">
<!--            <AppenderRef ref="localLog"/>-->
            <AppenderRef ref="infoLog"/>
        </Root>

        <logger name="org.springframework" level="info" additivity="true">
        </logger>
    </Loggers>

</Configuration>